if(!project.hasProperty('BINTRAY_PACKAGE')) project.ext.BINTRAY_PACKAGE = 'Kodein'
if(!project.hasProperty('ARTIFACT_NAME')) project.ext.ARTIFACT_NAME = 'kodein'
if(!project.hasProperty('DESCRIPTION')) project.ext.DESCRIPTION = 'Kotlin Dependency Injection'