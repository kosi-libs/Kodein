apply plugin: 'kotlin-platform-jvm'

dependencies {
    expectedBy project(":erased:kodein-di-erased-common")
    compile "org.jetbrains.kotlin:kotlin-stdlib:$kotlin_version"
    compile project(":core:kodein-di-core-jvm")

    testCompile project(':test-utils:test-utils-jvm')
    testCompile 'com.google.inject:guice:4.1.0'
    testCompile "org.jetbrains.kotlin:kotlin-reflect:$kotlin_version"
}

test {
    testLogging {
        events "passed", "skipped", "failed", "standardOut", "standardError"
    }
}

apply from: "../../gradle/dokka.gradle"

apply from: "../../gradle/kotlin-publish.gradle"

ext {
    pom_name = "Kodein-Erased-JVM"
    pom_description = "KODEIN Dependency Injection using erased types by default for the JVM & Android (optimized, but error-prone)"
}
apply from: "../../gradle/publication-upload.gradle"
