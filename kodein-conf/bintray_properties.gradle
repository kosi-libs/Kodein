if(!project.hasProperty('BINTRAY_PACKAGE')) project.ext.BINTRAY_PACKAGE = 'Kodein-Conf'
if(!project.hasProperty('ARTIFACT_NAME')) project.ext.ARTIFACT_NAME = 'kodein-conf'
if(!project.hasProperty('DESCRIPTION')) project.ext.DESCRIPTION = 'Kodein extension that enables a configurable Kodein object and a static global one true Kodein'