if(!project.hasProperty('BINTRAY_PACKAGE')) project.ext.BINTRAY_PACKAGE = 'Kodein-Android'
if(!project.hasProperty('ARTIFACT_NAME')) project.ext.ARTIFACT_NAME = 'kodein-android'
if(!project.hasProperty('DESCRIPTION')) project.ext.DESCRIPTION = 'Kodein extension for Android'