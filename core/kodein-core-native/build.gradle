apply plugin: 'konan'

apply from: '../../gradle/native-targets.gradle'

konanArtifacts {
    library("kodein-core-${project.version}") {
        srcFiles fileTree("src/main/kotlin")
        srcFiles fileTree("../kodein-core-common/src/main/kotlin")

//        extraOpts "-Xmulti-platform"
    }
}

ext {
    libName = "kodein-core-${project.version}"
    pom_name = "Kodein-Core-Native"
    pom_description = "KOtlin DEpendency INjection Core for Native Platforms"
}
apply from: "../../gradle/klib.gradle"
apply from: "../../gradle/klib-upload.gradle"
