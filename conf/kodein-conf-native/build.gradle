apply plugin: 'konan'

apply from: '../../gradle/native-targets.gradle'

konanArtifacts {
    library("kodein-conf") {
        srcFiles fileTree("../kodein-conf-common/src/main/kotlin")

        libraries {
            allLibrariesFrom project(':core:kodein-core-native')
        }
    }
}
